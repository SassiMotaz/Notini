<nav class="navbar navbar-expand-lg navbar-light bg-light">
	<div class="container-fluid">
		<a class="navbar-brand" href="#"><img src="/img/2.png" width="80" height="29"></a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarSupportedContent">
			<ul class="navbar-nav me-auto mb-2 mb-lg-0">
				<li class="nav-item">
					<a class="nav-link active" aria-current="page" href="{{path('app_home')}}">Home</a>
				</li>

				<li class="nav-item ">
					<a class="nav-link active " href="{{path('app_user_index')}}" role="button" aria-current="page">
						Utilisateurs
					</a>
				</li>
				<li class="nav-item ">
					<a class="nav-link active " href="{{path('app_role_index')}}" role="button" aria-current="page">
						Roles
					</a>
				</li>
				<li class="nav-item ">
					<a class="nav-link active  " href="{{path('app_etudiants')}}" role="button" aria-current="page">
						Étudiants
					</a>
				</li>
				<li class="nav-item  ">
					<a class="nav-link active  " href="{{path('app_matieres_index')}}" role="button" aria-current="page">
						Matières
					</a>
				</li>
				<li class="nav-item ">
					<a class="nav-link active " href="{{path('app_note_index')}}" role="button" aria-current="page">
						Notes
					</a>
				</li>

			</ul>

			<div>
				<div class="dropdown" style="margin-right: 80px;">
				{% if is_granted('IS_AUTHENTICATED_FULLY') %}
					<button class="btn dropdown-toggle" type="button" id="dropdownMenuButton1" data-bs-toggle="dropdown" aria-expanded="false">
						{{app.user.firstname}}
					</button>
					{% endif %}
					<ul class="dropdown-menu" aria-labelledby="dropdownMenuButton1">
						<li>
							<a class="dropdown-item" href="{{path('app_compte')}}">Mon Compte</a>
						</li>
						<li>
							<a class="dropdown-item" href="{{path('app_compte_edit')}}">Modifier mon profil</a>
						</li>
						<li>
							<a class="dropdown-item" href="{{path('app_update_password')}}">Confidentialité</a>
						</li>
						<li><hr class="dropdown-divider"></li>
						<li>
							<a class="dropdown-item" href="{{path('app_logout')}}">Déconnexion</a>
						</li>
					</ul>
				</div>
			</div>
		</div>
	</div>

</nav>
