
<form method="post" action="{{ path('app_matieres_delete', {'id': matiere.id}) }}" onsubmit="return confirm('Êtes-vous sûr de bien vouloir supprimer cet élément?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ matiere.id) }}">
    <div class="container">
    <button class="btn btn-primary mt-2 ">Supprimer</button>
    </div>
</form>
