{% extends 'base.html.twig' %}

{% block title %}Role{% endblock %}

{% block body %}
<div class="container mt-5">
    <h1>{{ role.title }}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ role.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ role.title }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-outline-primary" href="{{ path('app_role_index') }}">Retour à la liste</a>

    <a class="btn btn-outline-primary" href="{{ path('app_role_edit', {'id': role.id}) }}">Modifier</a>

    {{ include('role/_delete_form.html.twig') }}
    </div>
{% endblock %}
